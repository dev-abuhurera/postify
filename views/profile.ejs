<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
   <div class="w-full min-h-screen bg-gradient-to-br from-zinc-900 via-zinc-800 to-zinc-900 text-white">
    
        <div class="w-full h-16 bg-zinc-800/80 backdrop-blur-md shadow-md flex items-center px-6">
            <h1 class="text-white text-2xl font-extrabold tracking-wide">🚀 My App</h1>
        </div>

        
        <div class="w-full max-w-4xl mx-auto mt-12 p-6 bg-zinc-800/70 rounded-2xl shadow-lg border border-zinc-700">
            <h2 class="text-white text-2xl font-semibold mb-3">Welcome to My App</h2>
            <p class="text-zinc-300 mb-6 leading-relaxed">
                This is a simple application built with <span class="text-red-400 font-semibold">Node.js</span> and <span class="text-green-400 font-semibold">Express</span>.
            </p>
            <button class="bg-red-600 hover:bg-red-700 transition-colors px-6 py-3 rounded-xl shadow-md font-medium text-lg">
                Get Started
            </button>
        </div>

        <a class="bg-red-500 rounded-md px-3 py-2 text-sm inline-block mt-10 ml-10 " href="/logout"> Logout </a>

        <h3 class="text-xl font-semibold mt-6 text-center">Hello, <%= user.name %> 👋</h3>
    
        <form action="/post" method="post" 
        
            class="w-full max-w-md mx-auto mt-10 bg-zinc-800/70 p-6 rounded-2xl shadow-md border border-zinc-700 flex flex-col items-center gap-4">
            
            <textarea 
                class="w-2/3 resize-none bg-transparent border-2 border-zinc-700 rounded-md p-2 text-white"
                placeholder="What's on your mind? " name="content"></textarea>
            <input class="px-3  py-2 bg-blue-400 rounded-md" type="submit" value="Create new post">

        </form>

        <div class=" posts w-full max-w-4xl mx-auto mt-10 p-6 bg-zinc-800/70 rounded-2xl shadow-lg border border-zinc-900">
            <h3>Your Posts</h3>
            <div 
                class="postcontainer mt-4 flex flex-wrap justify-center gap-6">
                    
                <% user.posts.reverse().forEach(function(post){ %>
                  
                <div class="post w-1/3 max-w-4xl mx-auto mb-4  p-6 bg-zinc-700 rounded-2xl rounded-md shadow-lg border border-zinc-800">
                        
                    <h4 class="text-blue-600 text-xl mb-3"><%= user.username %></h4>
                    
                    <p class="text-sm tracking-tight text-zinc-400"><%= post.content %></p>
            
                   
                    <div class="btns flex gap-4">
                        <a class="text-blue-600 mt-3 tracking-tight" href="/like/<%= post._id %>">
                            <%= post.likes.indexOf(user._id) === -1 ? 'Like' : 'Unlike' %> 
                            <!-- if the likes array does not contain the user id, show like else unlike -->
                        </a>
                        <a class="text-zinc-500 mt-3 tracking-tight" href="/edit/ <%=post._id %>">Edit</a>
                    </div>
                    
                     <small class="mt-2 inline-block text-red-500 " ><%= post.likes.length%></small>
                
                </div>

                <% }) %>

            </div>
        </div>
</div>

</body>

</html>